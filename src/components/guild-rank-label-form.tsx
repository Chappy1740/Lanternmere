'use client'; import { useActionState } from 'react'; import { saveGuildRankLabel, type GuildMemberPortalState } from '@/app/(app)/guild-hall/actions'; const initialState:GuildMemberPortalState={error:null,success:null}; export function GuildRankLabelForm({guildId,rank,label}:{guildId:string;rank:number;label?:string}){const [state,action,pending]=useActionState(saveGuildRankLabel,initialState);return <form action={action} className="flex gap-2"><input type="hidden" name="guildId" value={guildId}/><input type="hidden" name="rank" value={rank}/><input name="label" defaultValue={label??`Rank ${rank}`} className="lodge-field w-40 px-2 py-1 text-sm"/><button disabled={pending} className="lodge-button-secondary px-2 text-sm">Save</button>{state.error&&<span className="text-red-400 text-xs">{state.error}</span>}</form>}
